<Window x:Class="UserControllCustomControls.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserControllCustomControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <local:CraftsmanInput x:Name="Craftsman"/>
        <Grid Grid.Row="1" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button x:Name="Cancel" Content="Cancel" Grid.Column="0" Template="{StaticResource RoundButton}" Click="Cancel_Click"/>
            <Button x:Name="Ok" Content="Ok" Grid.Column="1" Template="{StaticResource RoundButton}" Click="Ok_Click"/>
    </Grid>
    </Grid>
</Window>
